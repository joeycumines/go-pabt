re:

```
That prevents the planner from honestly supporting:

- complete BT Expansion;
- OBTEA-style optimal planning;
- admissible heuristic search;
- effect indexing;
- canonical closed sets;
- principled conflict resolution;
- formal verification.
```
You need to detail _why_, with references, and clear explanation, otherwise I cannot trust you. Do not just produce an explanation that fits - treat that as untrustworthy and reverify.

re:
````
## 1.3 `State.Actions` is neither a scalable grounder nor a complete grounding contract

```
Actions(failed T) ([]Action[T], error)
```

requires an eagerly materialized slice and cannot express:

- “there are more candidates”;
- “enumeration is complete”;
- “enumeration was suspended by budget”;
- “these are merely learned suggestions”;
- “this candidate depends on a geometric certificate”;
- infinite or continuous binding domains;
- continuation after an expensive sampling step;
- lower bounds on candidate cost;
- provenance;
- stale candidates after a model or world revision;
- action-schema indexing rather than full grounding.

This is particularly limiting for task-and-motion planning. PDDLStream demonstrates why continuous bindings and geometric relationships need lazy black-box streams whose outputs certify declarative facts, rather than an eager finite action slice.
````
You need to detail _what specifically requires each item_ and draw the links explicitly and clearly, with references. That final sentence is _not_ demonstrative, as it fails to provide specific enough descriptions referencing specific processes, mechanisms, etc - where to actually _look_ to find more details. That is similarly attempting to get me to take you at your word. Again, you need to detail _why_, with references, and clear explanation, otherwise I cannot trust you. Do not just produce an explanation that fits - treat that as untrustworthy and reverify.

re:
```
# 2. The non-negotiable design invariants
...
```
You guessed it. Many though not all of the items are insufficiently explained. The most egregious relate to planning models - world state, for example, needs actual detailed technical and fundamental / underlying analysis to make it clear **HOW** you reached that conclusion. All your explanations must be defensible in the academic arena AND teachable to new learners. As always **reassess** to ensure you reach the CORRECT (read: honest, complete, AI-slop-free) conclusion.

re: "4. Recommended package boundaries", no. One module, no new major version. That module is `github.com/joeycumines/go-behaviortree`. The `github.com/joeycumines/go-pabt` implementation can be considered complete, as it is believed to be faithful to the published algorithm, and has existing consumers. None of these modules will have notable external dependencies, and they are likely suitable to version together.

re: "5.1 Normative core", this seems quite solid, _however_, factors like:
```
`_ChildCount` and `BT_Child` are preferable to a hot `iter.Seq` or callback because a compiler can:

- preallocate exactly;
- calculate depth and edge count;
- validate the complete structure;
- assign stable occurrence IDs;
- build a compact CSR-style edge array;
- avoid iterator closure escape behavior;
- visit children without allocating.
```
MUST present CLEAR and VERIFIABLE reasoning, with AT MINIMUM relevant references supporting and (IF ANY EXIST THAT ARE POSSIBLY RELEVANT) detracting from these statements. You've over-claimed by default as a result of omitting this.

re:
```
## 6.2 Why `BT_Halt` is mandatory
```
The _concept_ is _potentially_ reasonable. Worthy of further analysis. Must be flagged as such - there will be a follow up to this follow up pertaining to streaming execution and linearisation of allocations, which will likely impact this concept.

Re:
```
Long cleanup belongs in optional `BT_Close(context.Context)` support.
```
Unlikely. The standard interfaces would be `interface { Shutdown(context.Context) error }` and `interface { Close() error }` which have well-defined semantics in Go (`io.Closer` in particular - note that it'd be distinct from the halt functionality).

Re: sections 6 to 8 (inclusive), impression is HIGH-LEVEL plausible, but not deeply validated. Comments:
- Deserves much deeper "big-picture" analysis and better more explicit framing particularly regarding the goals and non-goals of the architecture
- Avoid prepositions in all API surfaces (naming stylistic preference)

I have attached a **separate**, STRICTLY _inspirational_ (but subject-relevant) analysis. Drastic pivot is APPROVED if integrating aspect(s) from the attached analysis results in a better outcome. Keep in mind that was a COLD and substantially misaligned (but very relevant TECHNICAL) reference.

Judgement is reserved on much of the remaining document. Focus this next analysis on the areas mentioned, specifically. This is a _slightly_ different reframing - in addition to **the explicit context of the prior analysis focus re: one-shot-man's PA-BT architecture and follow ups, you are also answering:
````
!! **GOAL: Design a COMPLETE and WHOLLY compatible and interoperable API as an optional richer way to model behavior trees, with attached node data, inclusive of (if useful) leveraging Go 1.27's upcoming GENERIC METHOD SUPPORT, cutting-edge-BT-research - both ends of the cutting-edge spectrum, that is the VERY BEST, MOST POWERFUL, and MOST SOPHISTICATED examples, both current and FUTURE, in the areas of _automation and robotics_ AND _video game_, e.g. algorithms, approaches, and technical architectures, and all other facets of relevance** !!

Context: https://pkg.go.dev/github.com/joeycumines/go-behaviortree#section-documentation

Video game architecture framing:

Video games long exhausted the scale where it is viable to tick the full tree naively, and therefore operate on principles vaguely aligned with (N.B. PROBABLY WRONG - this is an example): linearising the tree and processing events as a major performance optimisation

Automation and robotics framing:

The following is based on a potentially incomplete but reasonably lengthy survey of the literature _currently_ available across the (potentially too-narrow) spectrum of behavior tree (BT) algorithms spanning foundational reactivity, rigorous search, probabilistic reasoning, and formal verification. At the execution baseline is the original **PA-BT** (Planning and Acting using Behavior Trees) for reactive, incremental tree expansion. To achieve algorithmic completeness and optimality, this foundation can be upgraded with **BT Expansion** for exhaustive symbolic state-space search and **OBTEA** (Optimal Behavior Tree Expansion Algorithm) for uniform-cost backward planning. For scalable, anytime problem-solving, **UHBTP** (benchmarked within the **BTPG** framework) applies domain-independent heuristics, while **HBTP** uses large language models to guide symbolic planners. When dealing with partial observability, **Belief Behavior Trees (BBT)** and **Active Inference** methodologies model sensing, uncertainty, and probabilistic local control. Finally, formal safety and deployment robustness are addressed by **CABTO** (ensuring grounding completeness), **BehaVerify** and **Fiacre** (for runtime and offline model checking), **LTLf synthesis** (for temporal mission goals), and **PDDL execution graph** transformations (for safe causal parallelization).
References:
* **PA-BT:** [ar5iv.labs.arxiv.org/html/1611.00230](https://ar5iv.labs.arxiv.org/html/1611.00230)
* **BT Expansion:** [ojs.aaai.org/index.php/AAAI/article/view/16755](https://ojs.aaai.org/index.php/AAAI/article/view/16755)
* **OBTEA:** [ijcai.org/proceedings/2024/0755.pdf](https://www.ijcai.org/proceedings/2024/0755.pdf)
* **BTPG & UHBTP:** [ijcai.org/proceedings/2025/0969.pdf](https://www.ijcai.org/proceedings/2025/0969.pdf)
* **Belief Behavior Trees (BBT):** [miccol.github.io/files/iros2020.pdf](https://miccol.github.io/files/iros2020.pdf)
* **CABTO:** [ojs.aaai.org/index.php/AAAI/article/view/40936](https://ojs.aaai.org/index.php/AAAI/article/view/40936)
* **BehaVerify:** [arxiv.org/abs/2208.05360](https://arxiv.org/abs/2208.05360)
* **Fiacre-based BTs:** [arxiv.org/pdf/2502.11904v1](https://arxiv.org/pdf/2502.11904v1)
* **LTLf Synthesis:** [arxiv.org/abs/2307.06399](https://arxiv.org/abs/2307.06399)
* **HBTP:** [arxiv.org/abs/2406.00965](https://arxiv.org/abs/2406.00965)
* **Active Inference & BTs:** [arxiv.org/pdf/2011.09756](https://arxiv.org/pdf/2011.09756)
* **PDDL Execution Graphs:** [arxiv.org/abs/2101.01964](https://arxiv.org/abs/2101.01964)
````
